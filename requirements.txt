# Production dependencies for Vercel (optimized for size)
# For local development with all features, use: pip install -r requirements-dev.txt

flask==3.0.0
flask-login==0.6.3
flask-mail==0.9.1
werkzeug==3.0.1
bcrypt==4.1.2
itsdangerous==2.1.2
python-dotenv==1.0.0

# Data processing - using older, smaller versions
pandas==2.0.3
numpy==1.24.3
openpyxl==3.1.2

# Optional for Groq AI (comment out if not needed)
# groq==0.4.1

# Note: Streamlit and plotly excluded from Vercel deployment
# IMDRF Insights feature requires local deployment or alternative hosting
